// SPDX-License-Identifier: MIT OR Apache-2.0

//! Tests for spawn_cancelable and restart counter functionality

use anyhow::Result;

#[tokio::test]
#[ignore] // Skip this test until we update it for the new API
async fn test_restart_counter_increments_on_panic() -> Result<()> {
    // This test needs to be updated for the new spawn_cancelable implementation
    Ok(())
}

#[tokio::test]
#[ignore] // Skip this test until we update it for the new API
async fn test_spawn_cancelable_macro() -> Result<()> {
    // This test needs to be updated for the new spawn_cancelable implementation
    Ok(())
}

#[tokio::test]
#[ignore] // Skip this test until we update it for the new API
async fn test_random_panics_with_recovery() -> Result<()> {
    // This test needs to be updated for the new spawn_cancelable implementation
    Ok(())
}
